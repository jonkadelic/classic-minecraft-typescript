export class KeyMapping {
    public name: string
    public defaultKey: number

    constructor(name: string, defaultKey: number) {
        this.name = name
        this.defaultKey = defaultKey
    }
}